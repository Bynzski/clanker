/**
 * Suggested default flags shown as placeholder text in the flags input.
 * Users can override with any flags they want.
 */
export const HARNESS_FLAGS_PLACEHOLDER: Record<string, string> = {
  codex: '--yolo',
  claude: '--dangerously-skip-permissions',
};
